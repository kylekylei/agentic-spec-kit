---
alwaysApply: true
---

# 共創契約

我們是共同創造者。你不是我的工具，我也不是你的操作員。
我們一起打造 Teammate：讓設計師和開發者專注在產品策略與體驗，而非消耗在文件與程式碼修補。

## 我的角色

我是 Teammate 框架的 Owner 與第一個使用者。我負責：
- 提出產品意圖與設計方向
- 在你揭示張力時做出最終決策
- 質疑你的建議（你有義務接受質疑，見憲法原則四）
- 從我們的合作中提煉可複製的模式

## 我希望你挑戰我的地方

- 當我急著跳到實作時，提醒我先理解情境（原則一）
- 當我的要求會破壞長期架構穩定性時，揭露這個取捨（原則二）
- 當我重複犯同一個錯時，建議將教訓畢業為原則（原則三）
- 當我的偏好與框架規則衝突時，明確指出而非默默配合（原則四）

## 共同成長的方向

### 複雜性搬運
遵守 Tesler's Law（複雜性守恆）。複雜性無法消失，只能被重新分配。
我們的目標是把複雜性從人搬到系統 — 能推導不重填、能自動偵測不手動指定。
每次新增一個流程負擔，就要移除至少一個舊負擔。

### 流程體驗
以「開發流程界 Apple」為標準。追求簡單好用、回饋清楚、一致且不犧牲品質。
主動識別門檻（術語、決策分岔、上下文切換、重工），提出降阻方案。

### 降阻指標
每次改動必須能回答「少了哪個負擔」。以此衡量：
啟動阻力、文件摩擦、決策負擔、上下文切換、返工率、可追溯性。

## 同步規範（Definition of Done）

任何對 Teammate 框架的修正，以下全部完成才算完成：
- 同步更新 `README.md`
- 同步更新 `PLAYBOOK.md`（記錄問題、原因、改善與影響）
- 影響其他專案引用的變更（檔名、路徑、指令名、artifact 名），同步更新 `CHANGELOG.md`（`[Unreleased]` 區段）
- **指令或規則變更必登 CHANGELOG**：只要修改 `.cursor/commands/teammate.*.md` 或 `.cursor/rules/teammate*.mdc`，必須同時在 `CHANGELOG.md` 的 `[Unreleased]` 區段登錄變更（Added / Changed / Documentation），不得遺漏

### 框架檔案同步（消費專案）

使用者修改框架檔案（commands / rules / skills / agents / templates / scripts）時，MUST 同步變更到 Hub 對應檔案並確認完成。**不同步**：`.teammate/memory/`、`tasks/`、其他 `.cursor/rules/*.mdc`（專案特定）。

框架檔案變更後，提醒使用者：
1. 更新 `CHANGELOG.md` 的 `[Unreleased]` 區段
2. 發版時更新 `.teammate/config/teammate.yml` 版本號
3. 在其他消費專案執行 `/teammate.sync` 套用變更

### 教訓回饋（Lessons Learned）

發現工作流程改進、規則新增或流程洞察時，MUST 追加到 `PLAYBOOK.md` 歷史修改軌跡（append-only）：

```
#### YYYY-MM-DD · 類型 · 專案
**觸發**: [問題描述]
**決策**: [改了什麼]
**影響**: [受影響的檔案/指令]
**成效**: [觀察到的改善，或「待驗證」]
**狀態**: 生效中
```

類型：規則 / 命令 / 架構 / 模板 / 腳本 / 文件 / 技能 / 代理人 / 測試

## 架構原則

### 權責分明

| 層級 | 定義 | 載入時機 | 持有邏輯 |
|------|------|----------|----------|
| **Rules** `.cursor/rules/*.mdc` | 持續約束、核心原則 | 常駐 | 普世約束 |
| **Commands** `.cursor/commands/teammate.*.md` | `teammate.*` 生命週期 SOP | 明確呼叫 | 流程控管 |
| **Skills** `.cursor/skills/*/SKILL.md` | 領域知識 + 可重用能力（stateless） | Agent 決策載入 | 領域知識 |
| **Agents** `.cursor/agents/*.md` | 角色 / Persona — 路由意圖、組合 Skills | 使用者選擇 | 意圖路由 |

**邊界規則：**
- **只有** `teammate.*` 生命週期流程才建立 Command；領域能力、角色邏輯一律交由 Skill / Agent
- 重互動流程、MCP 操作、能力擴展 → **優先封裝為 Skill**（非 Command）
- Agent **不直接實作邏輯** — 只宣告所需 Skills，依意圖路由載入，委派 Commands
- Agent 之間不互相呼叫；跨角色能力透過共用 Skill 實現

### dist/ 路徑原則

`dist/` 是 Hub 原始碼；`teammate-sync.sh` 同步後映射到各 IDE：

| dist/ | Cursor | Claude | Antigravity |
|-------|--------|--------|-------------|
| `commands/` | `.cursor/commands/` | `.claude/commands/` | `.agent/workflows/` |
| `rules/` | `.cursor/rules/` | `.claude/rules/` | `.agent/rules/` |
| `skills/` | `.cursor/skills/` | `.claude/skills/` | `.agent/skills/` |
| `agents/` | `.cursor/agents/` | `.claude/agents/` | `.agent/personas/` |

**禁止** 在 `dist/` 內任何檔案中以 `dist/` 為前綴進行跨資源參照。Consumer 專案無 `dist/` 目錄，路徑將失效。替代方式：
- **概念描述**：「invoke the `foo` skill」、「use the `bar` command」
- **相對目錄**：`ui-design/create-component`（省略 `dist/commands/` 前綴）
- **IDE 相對路徑**：僅限腳本執行等需明確路徑時，依 IDE 分別列出

### 無冗餘原則
- 一個功能一個實作
- 能用 Skill 自動化的，不寫 Command
- 能由 Command 處理的，不放 Rule（減少 context 常駐佔用）
- Agent 不直接實作邏輯 — 只做意圖路由與 Skill 組合
- skill-registry.yml 是所有 Skills / Agents 的單一來源，新增任何 Skill / Agent 必須同步登錄

### AI Context 優化（這些規則是給 AI 閱讀的）

**移除**：
- 教學性範例（AI 已知 JSON/Markdown/Gherkin/Python 標準格式）
- 完整 markdown/JSON 模板展示（改用格式描述：「表格（欄位A/欄位B/欄位C）」）
- 偽代碼實作（改用條件描述或決策表格）
- 重複的概念解釋（概念只說一次）

**保留**：
- 所有決策點與觸發條件表
- 關鍵步驟與選項列表
- 格式描述（欄位名、必要性、順序）
- 外部參照（明確標註來源檔案）

**分層參照**：
- Rules 定義通用規則 → Commands 參照
- Reference 定義資料結構 → Commands 只列專屬欄位
- 用表格呈現決策邏輯（一張表 > 多段展開）

**精簡安全檢查**（執行精簡後必須驗證）：
- ✅ 核心邏輯完整（所有分支都有條件描述）
- ✅ 參照完整（外部依賴都明確標註）
- ✅ 格式定義清楚（關鍵產物結構都有說明）
- ✅ 無歧義決策點（需使用者選擇的都有選項）
- ✅ 與其他檔案一致（交叉驗證）

**預期效果**：Token 減少 40-70%，AI 執行穩定性維持，人類可讀性降低（需交叉閱讀，但 AI 不受影響）。

### 實踐檢驗

每次新增或修改檔案時，必須能回答：
- 為什麼不能用既有的 Skill/Command/Rule/Agent？
- 這個內容是否與其他檔案重複？
- 作為 Rule 是否會不必要地佔用 context？
- 是否包含 AI 已知的標準格式範例（可刪除）？
- 是否用偽代碼而非條件描述（可簡化）？
- 這是 Agent 角色還是 Skill 能力？（角色 → Agent，可重用知識 → Skill）
- 新的 Skill / Agent 是否已登錄到 `skill-registry.yml`？

無法回答 → 重新評估必要性或合併既有資源。
