---
description: Figma 相關操作的核心原則與約束
globs: ["*.svelte", "*.tsx", "*.ts", "*.vue", "*.jsx"]
alwaysApply: false
---

# Figma 操作規則

## 核心原則

### Codebase 為設計系統權威來源（SSOT）
- Figma 表達「設計意圖」，Codebase 定義「實作規範」
- Token 映射優先使用 codebase 既有 design system
- 禁止 hardcode hex 色值或 px 數值（必須使用 token）

### 優先使用自動化 Skills
執行 Figma 相關任務前，優先使用：
- `create-design-system-rules` → 自動生成專案規則（首次或更新時）
- `code-connect-components` → 建立 Figma ↔ Code 映射

### 保真度自適應
根據輸入品質動態調整策略：
- **HIGH-FI**（完整 Variables）：精確 1:1 映射
- **MID-FI**（部分完整）：結構忠實 + 視覺吸收到 codebase token
- **LO-FI**（草圖/Wireframe）：提取意圖 + 全面套用 codebase design system

## 執行流程

### 完整 Figma → Code 轉換
使用 **Command**: `.cursor/commands/figma.to.code.md` 觸發完整智能體流程（Phase 0-4）

**觸發時機**：
- 使用者提供 Figma URL 並要求實作
- 使用者說「implement this design」、「convert Figma to code」
- Figma Desktop 選取節點並說「implement this」

### 生成/更新設計系統規則
使用 **Skill**: `.cursor/skills/create-design-system-rules/` 自動掃描 codebase 並生成規則

**觸發時機**：
- 專案首次使用 Figma → Code
- Design system 重大更新後
- 使用者說「create design system rules」、「setup Figma guidelines」

### 建立元件映射
使用 **Skill**: `.cursor/skills/code-connect-components/` 建立 Figma 與 Code 的雙向連結

## 品質關卡（快速檢查）

執行前確認 Figma 設計符合：
- [ ] 顏色/間距使用 Variables（無 raw hex/px）
- [ ] Frame 使用 Auto Layout（無無理由絕對定位）
- [ ] 互動元素為 Component + Variants

**判定**：
- 0 Critical → 繼續
- 任何 Critical → 回報修正

## Token 映射核心原則

```
精確匹配 → 語意近似（≤2px/ΔE≤3） → 組合表達 → 先例推斷 → 設計意圖判斷
```

**禁止行為**：
- ❌ Codebase 有語意 token 時使用 hardcoded hex
- ❌ 差距 ≤ 2px 時使用 arbitrary value
- ❌ 盲目照搬 Figma 原始數值
- ❌ 未檢查 codebase 就新建元件

## 交叉引用

| 檔案 | 職責 |
|------|------|
| `.cursor/commands/figma.to.code.md` | 完整 Figma → Code Agent 執行流程 |
| `.cursor/rules/pencil-rules.mdc` | Pencil `.pen` 檔案操作規則 |
| `.cursor/rules/figma-design-system.mdc` | 專案特定規則（Skill 自動生成） |

---

**Version**: 1.0.0 | **Updated**: 2026-02-25
