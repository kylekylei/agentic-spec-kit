---
globs:
  - ".cursor/rules/teammate-rules.mdc"
  - ".cursor/commands/teammate.init.md"
  - ".cursor/commands/teammate.align.md"
  - ".cursor/commands/teammate.plan.md"
  - ".cursor/commands/teammate.execute.md"
  - ".cursor/commands/teammate.review.md"
  - ".cursor/commands/teammate.audit.md"
  - ".cursor/commands/teammate.helpme.md"
alwaysApply: false
---

# Teammate Sync Rule

**Teammate Hub**: `[TEAMMATE_HUB_PATH]`

## Sync: Framework Files

使用者修改 globs 定義的檔案時，MUST 同步變更到 Hub 對應檔案，並確認完成。不同步專案特定檔案（`.teammate/memory/`、`tasks/`、其他 `.cursor/rules/*.mdc`）。

## Sync: Version Tracking

框架檔案變更後，提醒使用者：
1. 更新 `[TEAMMATE_HUB_PATH]/CHANGELOG.md` 的 `[Unreleased]` 區段
2. 發版時更新 `[TEAMMATE_HUB_PATH]/.teammate/config/teammate.yml` 版本號
3. 在其他專案執行 `/teammate.helpme migrate` 套用變更

## Sync: Lessons Learned

發現工作流程改進、規則新增或流程洞察時，MUST 追加到 `[TEAMMATE_HUB_PATH]/PLAYBOOK.md` Section 1「歷史修改軌跡」（append-only）：

```
#### YYYY-MM-DD · 類型 · 專案
**觸發**: [what went wrong or could be better]
**決策**: [what was changed]
**影響**: [affected files/commands]
**成效**: [observed improvement, or 待驗證]
**狀態**: 生效中
```

類型：規則 / 命令 / 架構 / 模板 / 腳本 / 文件 / 技能 / 測試